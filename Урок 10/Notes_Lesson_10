УРОК 10. Работа с DOM.

МЕТОД УДАЛЕНИЯ remove 

const lists = document.querySelectorAll('.list')
const elems = document.querySelectorAll('.elems')

console.log(lists);
console.log(elems);


elems[2].remove()
elems[4].remove()// удаляем 2 и 4 по счету элементы, НО удалены только из ВЕРСТКИ!!

//Эти элементы можно переиспользовать в другом месте 

МЕТОД ДОБАВДЕНИЯ append
В метод append добавляем элемент, который хотим положить внутhь элемента, к которому обращался 
Метод ПЕРЕМЕЩАЕТ содержимое

lists[1].append(elems[2]);
lists[1].append(elems[4]);


